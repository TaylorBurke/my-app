import React from "react";
import {AppState} from "../interface/AppState";

const TemplateManager = (state: AppState) => {

    return (
            <div>
                <button>New Template</button>

                {/*state.mapAllTemplatesByLabel*/}
            </div>
    )
}

export default  TemplateManager;